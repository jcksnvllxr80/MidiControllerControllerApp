{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core events, window, and app APIs. Custom commands (scan/connect/etc.) need no extra permission in Tauri v2.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
